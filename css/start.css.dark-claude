/* ============================================================
   DARK MODE — start.css
   Design tokens matched to example-dark-mode.html
   ============================================================ */

:root {
  --bg-page:        #1a1a1a;
  --bg-primary:     #212121;
  --bg-secondary:   #2a2a2a;
  --bg-tertiary:    #333333;
  --text-primary:   #ececec;
  --text-secondary: #a0a0a0;
  --text-tertiary:  #666666;
  --border:         rgba(255, 255, 255, 0.10);
  --border-md:      rgba(255, 255, 255, 0.18);
  --radius-md:      8px;
  --radius-lg:      12px;
  --accent-blue:    #378ADD;
}

*, *::before, *::after { box-sizing: border-box; }

body {
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
  font-size: 14px;
  background-color: var(--bg-page);
  color: var(--text-primary);
  margin: 0;
  padding: 0;
  line-height: 1.6;
}

/* --- Header --- */
.top-header {
  background-color: var(--bg-primary);
  color: var(--text-primary);
  padding: 15px 20px;
  margin-bottom: 0;
  border-bottom: 0.5px solid var(--border);
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.top-header h1 {
  margin: 0;
  font-size: 16px;
  font-weight: 500;
  color: var(--text-primary);
}

/* Back button — ghost style */
.btn-back {
  background: transparent;
  border: 0.5px solid var(--border-md);
  border-radius: var(--radius-md);
  color: var(--text-primary);
  text-decoration: none;
  padding: 7px 14px;
  font-size: 13px;
  font-weight: 500;
  transition: background 0.15s;
}

.btn-back:hover {
  background: var(--bg-secondary);
  color: var(--text-primary);
}

/* --- Nav Link / Control Buttons --- */
.controls {
  padding: 16px 20px;
  border-bottom: 0.5px solid var(--border);
  background-color: var(--bg-primary);
}

.controls a {
  display: inline-flex;
  align-items: center;
  padding: 7px 14px;
  margin-right: 8px;
  background: var(--bg-secondary);
  color: var(--text-primary);
  text-decoration: none;
  border-radius: var(--radius-md);
  border: 0.5px solid var(--border-md);
  font-size: 13px;
  font-weight: 500;
  transition: background 0.15s, border-color 0.15s;
}

.controls a:hover {
  background: var(--bg-tertiary);
  border-color: rgba(255, 255, 255, 0.28);
}

/* --- Two-Panel Layout --- */
.container {
  display: flex;
  width: 100%;
  height: calc(100vh - 110px); /* viewport minus header + controls */
  gap: 15px;
  padding: 15px;
}

.left-panel, .right-panel {
  flex: 1;
  display: flex;
  flex-direction: column;
  min-width: 0;
  height: 100%;
  background: var(--bg-primary);
  border: 0.5px solid var(--border);
  border-radius: var(--radius-lg);
  overflow: hidden;
  box-shadow: 0 4px 6px rgba(0, 0, 0, 0.3);
}

/* --- Iframe / Embed Dimensions --- */
#left-container {
  border: none;
  width: 100%;
  height: 100%;
  background: var(--bg-primary);
}

/* Right panel sub-sections */
.file-content {
  margin-bottom: 10px;
}

.static-content {
  flex-grow: 1;
}

.right-panel embed {
  border: none;
  border-bottom: 0.5px solid var(--border);
  width: 100%;
  height: 100px;
  background: var(--bg-secondary);
}

.right-panel object {
  border: none;
  width: 100%;
  height: 100%;
  background: var(--bg-primary);
}
